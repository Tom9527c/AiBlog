# 即刻短文合并验证

2026-09-22。后台 essays 专用卡片管理、混合媒体发布器、记录时间/地点、草稿/定时、置顶/标签、页面设置；博客 /essay/ 独立瀑布流与 /fcircle/ 重定向。

## 数据
- nest-admin/scripts/blog-merge-essays.cjs 已执行，首次迁移 1 条旧朋友圈，合并后 2 条短文。第二次 inserted=0。
- 备份：nest-admin/.blog-backups/essays-before-2026-09-22T07-14-34-279Z.json（权限 0600）。
- 保留 blog_moment 原始表和 blog_essay_migration 映射。旧 moments 公共读取及管理写入关闭；媒体引用转移至 essays，避免旧访问规则继续提供文件访问。
- 原菜单同动作权限迁移，旧后台菜单隐藏，旧页面跳转。

## 验证结果
- 后端 metadata/受限媒体/站点相关测试 78 项通过，随后新增的旧 moments 禁止公开读取和锁定摘要脱敏测试通过（essay 专项 13 项）。
- 真实 API 回归 54 项通过：媒体存储/授权、混合媒体保存、正文投影、记录时间地点、草稿/定时过滤、密码解锁、单条评论和旧写入口。测试数据已清理。
- 博客完整测试 107 项通过，新增卡片交互测试通过（混合媒体、无自动播放、日期地点、评论目标）。
- Nest build、Vue typecheck、博客 build、后台 build:blog 通过。
- 桌面浏览器 1280px：页头 304px，三列卡片，现有两条数据正常。
- 手机浏览器 390px：页头和卡片宽 358px，左右各 16px，单列，document.scrollWidth=390 无横向溢出。
- 后台浏览器确认专用发布器、混合媒体入口、日期地点及发布按钮，当前旧数据两条可编辑。
- 独立审查修复：锁定标题摘要脱敏；旧 moment 公开读取封闭；嵌套媒体上传期间禁止发布。

## 限制
上传沿用每文件 25 MB，支持 JPG/PNG/GIF/WebP/AVIF/MP3/MP4；直接播放需可播放文件链接，第三方分享页面使用链接卡片。页头不复制参考站素材，默认使用已有站点封面，可在短文页面设置替换。完整测试媒体往返由隔离 API 测试验证，未改动真实短文加入示范媒体。构建原有大包和 UnoCSS 图标警告仍存在。
