# Comments Implementation Plan

> Execute using subagent-driven-development for the backend task, with local UI integration and independent final review. Existing workspace has no root git repository; preserve files in place.

**Goal:** Reference-style guestbook and shared moderated guest comments.
**Architecture:** Dedicated comments service and endpoints over existing blog_comment; shared React UI; dedicated Vue review queue.
**Tech Stack:** NestJS/TypeORM, React, Vue/Naive UI.
**Spec:** docs/superpowers/specs/2026-09-22-comments-design.md

## Global constraints
Preserve live data; no synchronize/migrations. All public submissions pending. No public private metadata. Existing shared header behavior preserved.

### Task 1: Backend comments
- [x] Tests for guest pending -> approval -> public, forged identity, replies, reactions, private projection, denied targets.
- [x] Dedicated public GET comments with page/pageSize/order latest|oldest|popular and targetKind/targetId; returns items (roots with replies), total (root count), commentCount, page/pageSize. Root/replies provide existing Content fields plus metadata authorName/authorAvatar/authorWebsite/isOwner/browser/os/replyToName, likes/dislikes/myVote (top-level); email and voter data private.
- [x] POST comments body,parentId?,nickname?,email?,website?,metadata targetKind/targetId, honeypot?; always pending response. POST comments/:id/reaction {value:1|-1|0}; returns likes/dislikes/myVote. Cookie or stable opaque voter header supported, with rate limits.
- [x] GET admin/comments query moderation pending|approved|rejected, keyword,targetKind,page/pageSize -> items,total,page,pageSize,counts. POST admin/comments/moderate {ids:number[],status:approved|rejected|pending,reason?:string}; POST admin/comments/:id/reply {body:string} -> approved admin reply. Existing delete endpoint remains guarded.
- [x] Unit tests/build and API integration using isolated temporary data, clean up.

### Task 2: Shared frontend and guestbook
- [x] New features/comments/Comments.tsx, CommentBody.tsx, comments.css and Guestbook.tsx; legacy re-export; routing guestbook.
- [x] Guest form, Markdown preview, emoji/image URL insertion; submit->pending notice; reply target, sorting, refresh, reactions, root pagination, mobile styling.
- [x] Envelope interaction without hover flicker, tap friendly. Guestbook author uses configured site identity.
- [x] Tests for guest submit/request fields, rendering threads/private output, loading and failures; build; browser desktop/mobile.

### Task 3: Dedicated admin queue
- [x] Replace views/blog/comments/index.vue with review tabs/filter/search, cards, selection/bulk approve/reject, detail, reply and delete.
- [x] Preserve page header settings entry. API permission failures surfaced; pending buttons and confirmations prevent accidental duplicate requests.
- [x] typecheck/build and browser verification.

### Task 4: Verification
- [x] Independent security/functional review, address important findings.
- [x] Verify tests/builds, document outcomes and exact limitations.
